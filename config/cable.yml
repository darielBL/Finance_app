# config/cable.yml
production:
  adapter: solid_cable
  connects_to:
    database:
      writing: :cable
  polling_interval: 0.1
  message_retention: 1.day

development:
  adapter: solid_cable
  connects_to:
    database:
      writing: :cable
  polling_interval: 0.1
  message_retention: 1.day

test:
  adapter: solid_cable
  connects_to:
    database:
      writing: :cable
  polling_interval: 0.1
  message_retention: 1.day